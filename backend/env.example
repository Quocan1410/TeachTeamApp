# Backend reads environment from the project ROOT .env file.
# Copy and configure:  cp ../env.example ../.env
#
# See ../env.example for all variables (DB_*, BACKEND_JWT_SECRET, ALLOWED_ORIGINS, etc.)
#
# Dev accounts (after npm run db:reset) — 6 users, Password123! each:
#   Lecturers: jane.morrison@, marcus.chen@, priya.sharma@lecturer.edu.au
#   Candidates: alex.nguyen@, samira.patel@, james.oconnor@candidate.edu.au
#   Admin: admin@admin.com / ADMIN_PASSWORD from root .env
#
# Forgot-password security answers (all seeded users): see DEMO_SECURITY_ANSWERS in
# src/seeds/bootstrapDataset.ts or the table in ../env.example.
